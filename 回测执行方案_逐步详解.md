# 开源金工交易行为因子复现 — 逐步执行方案（学习导向）

> 配套文档：`开源金工交易行为因子_文章总结与回测方案.md`（讲"是什么/结果是什么"）。
> 本文档讲**"每一步具体做什么、为什么做、产出什么、对应哪段程序、你能学到什么、怎么自检"**。
> 目标不是最快出结果，而是让你系统性理解一条量化因子流水线的每个环节。

---

## 0. 如何使用本文档

- 每个"阶段"是一层，层与层之间是**单向数据流**：上一层的产出是下一层的输入。
- 每个步骤统一按六块写：
  - **为什么**：这一步解决什么问题，不做会怎样
  - **做什么**：具体操作
  - **产出**：落盘的文件 + 格式
  - **对应程序**：写在哪个脚本/函数里
  - **学习点**：这一步能让你学到的编程/数据分析概念
  - **自检**：怎么确认这一步做对了
- 建议执行顺序就是文档顺序。**每完成一层，先自检通过再进下一层**——错误在早层不查，会在回测层放大成看不懂的结果。

---

## 1. 全局架构总览

### 1.1 数据流（一张图记住整条线）

```
原始数据(data_raw)  ← 已完成
      │
      ▼
阶段A 清洗层(data_clean)      复权价 + 可投资域 + 月度面板 + 行业时点表
      │
      ▼
阶段B 因子层(factors)         每个因子一张"股票×日→因子值"表
      │
      ▼
阶段C 单因子回测(backtest)    IC序列 + 分组净值 + 绩效指标
      │
      ▼
阶段D 合成因子               方向对齐 + 标准化 + ICIR加权
      │
      ▼
阶段E 样本外诊断             2020-2026 OOS + 疲软期归因
      │
      ▼
阶段F 可视化 + 网站          净值/IC/分池图 → 静态网站 → GitHub
```

### 1.2 目录结构（各层产出的落脚点）

```
trove_开源金工/
├── data_raw/          原始数据（已下好，只读，不动）
├── data_clean/        阶段A 产出：干净的复权价/掩码/面板
├── factors/           阶段B 产出：各因子值表
├── backtest/          阶段C-E 产出：IC、净值、绩效、图表数据
├── website/           阶段F：网站源文件
├── script/            下载脚本（已完成）
├── src/               ★新增★ 因子/回测的核心代码模块（可复用）
└── notebooks/         ★新增★ 探索、自检、手算核对用的 Jupyter
```

**为什么分 `src/` 和 `notebooks/`：** `src/` 放**可复用、被反复调用**的函数（算因子、算IC…）；`notebooks/` 放**一次性探索和自检**（画个图看看、抽几只股票手算核对）。这是数据分析项目的标准分法——库代码和探索代码分开，库代码才能被测试、被复用。

### 1.3 一个贯穿始终的核心数据结构：面板（panel）

几乎每一步的数据都是一张**二维表**，两个维度是：
- **行 = 交易日期**（trade_date）
- **列 = 股票代码**（ts_code）
- **值 = 某个量**（价格、因子值、收益…）

```
              000001.SZ  000002.SZ  600000.SH ...
20100104        0.023      0.015      0.031
20100105        0.019      0.022      0.028
...
```
这叫"**宽表 / panel**"。pandas 里就是一个 `DataFrame`，`index` 是日期、`columns` 是股票。
**为什么统一用这个结构：** 因子计算是"每天对所有股票排序"（沿行做横截面运算），收益对齐是"整体下移一格"（沿列做时间运算）——宽表让这两种运算都变成一行 pandas 代码。你后面会反复用到 `.rank(axis=1)`、`.shift()`、`.mean(axis=1)`。

---

## 阶段A：数据清洗层（data_clean）

> **这一层是所有因子共用的地基，只建一次。** 跳过它、把清洗逻辑塞进每个因子里，你会重复写四遍、还会不一致。

### A.1 后复权价格面板

- **为什么**：算收益率必须用复权价。20天窗口里若发生分红送股，未复权的收盘价会突然跳一下——那个跳空不是真实涨跌，会把收益、动量、价格排序全算错。复权就是把这些跳空"抹平"，还原成连续的价格序列。
- **做什么**：
  1. 读 `data_raw/daily/*.parquet`（open/high/low/close/pre_close）+ `data_raw/adj_factor/*.parquet`（adj_factor）
  2. 后复权价 = 原始价 × adj_factor（open/high/low/close 各乘一次）
  3. 转成宽表：每个价格字段一张 panel（复权close、复权high、复权low、复权open）
- **产出**：`data_clean/px_close_hfq.parquet`、`px_high_hfq.parquet`、`px_low_hfq.parquet`、`px_open_hfq.parquet`（都是 日期×股票 宽表）
- **对应程序**：`src/data_clean.py` 里的 `build_adjusted_panels()`
- **学习点**：
  - **复权原理**：前复权 vs 后复权的区别（后复权不改变历史、适合回测；前复权最新价=真实价、适合看图）
  - **long → wide 转换**：`df.pivot(index='trade_date', columns='ts_code', values='close')`
  - **为什么振幅因子例外**：振幅 = high/low − 1，复权因子在分子分母抵消，所以振幅用不用复权都一样——但**排序用的 close 必须复权**（否则除权日的假跳空会污染高低价分组）
- **自检**：抽一只有分红的股票（如某年 6 月除权的票），画出复权前后 close 曲线——复权后应无突兀跳空；对比 Tushare 的 `pro_bar(adj='hfq')` 抽查几个点是否一致。

### A.2 可投资域掩码（universe mask）

- **为什么**：不是每只股票每天都能买。停牌的买不进，一字涨停的封死板买不到、一字跌停的卖不掉，ST 有退市风险，次新股（上市不满60日）流动性和定价异常。这些票若混进回测，会产生**买不到的虚假收益**。掩码就是每天给每只股票打一个"能不能选"的标记。
- **做什么**：构造一张和价格 panel 同形状的**布尔宽表**，`True`=当日可投资：
  1. **剔 ST**：用 `namechange` 表，判断该股当日名称是否含 "ST"/"*ST"
  2. **剔次新**：用 `stock_basic.list_date`，上市满 60 个交易日才为 True
  3. **剔停牌**：用 `suspend_d`，当日停牌为 False
  4. **剔一字涨跌停**：用 `stk_limit` + daily，当日 `high==low` 且 `close==up_limit`(或 down_limit) 为 False
- **产出**：`data_clean/investable_mask.parquet`（日期×股票 的 True/False 宽表）
- **对应程序**：`src/data_clean.py` 里的 `build_investable_mask()`，内部拆成 `_mask_st / _mask_new / _mask_suspend / _mask_limit` 四个小函数各管一条
- **学习点**：
  - **布尔掩码（boolean masking）**：数据分析核心技能，`factor[~mask] = np.nan` 一句话把不可投资的票剔掉
  - **区间判断**：ST 是"某段时间内"含 ST（用 namechange 的 start_date/end_date 判断当日落在哪个区间）——这是"时点还原"的典型操作
  - **多条件组合**：`mask = mask_st & mask_new & mask_suspend & mask_limit`
- **自检**：随机抽 3 个交易日，手动核对几只已知停牌/ST/次新的股票掩码是否正确；统计每日可投资股票数量画条时间线——应从 2010 年约 1800 只平滑增长到 2026 年约 5000 只，无诡异断崖。

### A.3 调仓日历与月度面板

- **为什么**：因子是每天都能算的（日频），但我们**月度调仓**。所以要确定"每个月在哪一天调仓"，并把日频因子在这些调仓日"取样"成月频。同时要把每只股票"从这个调仓日到下个调仓日"的收益算出来，作为检验因子的标签（未来收益）。
- **做什么**：
  1. 从 `trade_cal` 取每个月最后一个交易日（或第一个，二选一固定）作为**调仓日**
  2. **未来一期收益**：`fwd_ret[t] = 复权close[下个调仓日] / 复权close[本调仓日] − 1`
  3. 得到一张"调仓日 × 股票"的未来收益面板
- **产出**：`data_clean/rebalance_dates.parquet`（调仓日列表）、`data_clean/fwd_ret_monthly.parquet`（调仓日×股票 的未来一月收益）
- **对应程序**：`src/data_clean.py` 里的 `get_rebalance_dates()` 和 `build_forward_returns()`
- **学习点**：
  - **重采样/取样（resample）**：从日频取月末点
  - **`shift()` 与前视偏差**：未来收益是"下一期"的值，用 `.shift(-1)` 把未来对齐到当前；★这里最容易埋下 look-ahead bug——务必想清楚正负号
  - **时间对齐**：因子在 T 日、收益在 T→T+1，两者错开一期才是"预测"
- **自检**：抽一只股票、一个调仓日，手算 `下月close/本月close−1`，和面板里的值对上；确认因子日期永远 ≤ 收益起点日期（没有用到未来）。

### A.4 行业与指数成分的时点表

- **为什么**：行业中性化、分池回测都需要"某只股票在**某个时点**属于哪个行业/在不在某指数里"。我们下载时已修正为带 in_date/out_date 的历史记录（避免前视），这一步把它整理成"给定日期能快速查询归属"的形式。
- **做什么**：
  1. 行业：把 `sw_l1_members`（含 in_date/out_date）整理成一个函数 `industry_at(code, date)`
  2. 成分：把 `index_weight`（月度快照）整理成"某调仓日某指数的成分列表"
- **产出**：`data_clean/industry_panel.parquet`（调仓日×股票→行业代码）、`data_clean/index_members/*.parquet`
- **对应程序**：`src/data_clean.py` 里的 `build_industry_panel()`、`get_index_members(index, date)`
- **学习点**：
  - **时点查询（point-in-time / as-of join）**：`in_date <= date < out_date` 的区间匹配，量化防前视的核心技术
  - **稀疏→稠密**：成分是月度快照，要 forward-fill 到每个调仓日
- **自检**：用我们之前验证过的 000004.SZ（房地产→医药生物→计算机）抽查几个日期的行业是否正确切换。

---

## 阶段B：因子计算层（factors）

> 每个因子的产出都是**同一种形状**：`调仓日 × 股票 → 因子值` 的宽表。形状统一，阶段C 的回测代码才能一套通用、四个因子复用。

### B.1 理想振幅因子 V（第一个做，纯日线，最干净）

- **因子逻辑**（回顾）：高价态振幅承载负向 alpha，低价态振幅是噪声，作差提纯。
- **做什么**（每个调仓日、每只股票）：
  1. 回看最近 20 个**有效交易日**（用 A.2 掩码剔停牌/一字板）
  2. 每日振幅 `amp = high/low − 1`（复权无所谓）
  3. 按当日**复权 close** 排序，取最高 25%（λ）的天 → 均值 = `V_high`；最低 25% → `V_low`
  4. `V = V_high − V_low`
  5. 有效日不足 10 天 → 该股当期置 NaN
- **产出**：`factors/ideal_amplitude.parquet`（调仓日×股票→V）
- **对应程序**：`src/factors/ideal_amplitude.py` 里的 `compute(px_high, px_low, px_close, mask, dates)`
- **学习点**：
  - **滚动窗口（rolling window）**：20日回看，`rolling(20)` 或手动切片
  - **分位数分组**：`np.quantile` 或排序后取头尾切片
  - **NaN 传播控制**：有效数据不足时主动置 NaN，而不是让它算出一个不可靠的值
- **自检**：抽 1 只股票、1 个调仓日，把这 20 天的 high/low/close 拉出来，Excel 手算 V_high、V_low、V，和程序对上（这是最重要的一次核对，框架对不对全看它）。

### B.2 理想反转因子 M（moneyflow 大单占比替代口径）

- **因子逻辑**（回顾）：反转之力源于大单；用大单占比切出"大单主导日"。
- **做什么**（每个调仓日、每只股票）：
  1. 回看最近 20 个交易日
  2. 每日 `大单占比 = (buy_lg+sell_lg+buy_elg+sell_elg) / 当日总成交额`（用 moneyflow）
  3. 大单占比最高的 10 天，`pct_chg` 加总 → `M_high`；最低 10 天 → `M_low`
  4. `M = M_high − M_low`
- **产出**：`factors/ideal_reversal.parquet`
- **对应程序**：`src/factors/ideal_reversal.py`
- **★额外一步（替代口径验证）**：用 iFinD 试用账号 2022–2026 的真·成交笔数，在重叠窗口对比"真·平均单笔成交金额"与"大单占比"切出的大单日重合度，写进报告。对应 `notebooks/validate_reversal_proxy.ipynb`。
- **学习点**：`argsort` 取头尾索引；替代口径的敏感性验证方法论。
- **自检**：同 B.1，抽样手算；额外确认大单占比 ∈ [0,1]。

### B.3 聪明钱因子 Q（依赖分钟数据，后做）

- **依赖**：需要分钟数据（Tushare `stk_mins`），还没下。先占位，等分钟数据方案落地。
- **做什么**（回顾）：过去10日分钟数据 → `S=|R|/V^0.25` → 按S排序累计成交量前20%为聪明钱 → `Q=VWAP_smart/VWAP_all`。
- **产出**：`factors/smart_money.parquet`
- **对应程序**：`src/factors/smart_money.py`
- **学习点**：分钟级数据的分组聚合；VWAP 计算；大数据分批处理。

### B.4 APM 因子（依赖分钟数据，后做）

- **依赖**：下午段收益需 13:00 盘中价（分钟数据）；隔夜段日线可算。
- **做什么**（回顾）：隔夜/下午收益 → 40组回归取残差 → stat → 对 Ret20 横截面正交。
- **产出**：`factors/apm.parquet`
- **对应程序**：`src/factors/apm.py`
- **学习点**：逐股票时序回归（`np.polyfit`/`statsmodels`）；横截面正交化。

---

## 阶段C：单因子回测层（backtest）

> 输入 = 一张因子值表 + 未来收益表；输出 = 这个因子好不好的全套证据。**四个因子共用这一套代码**。

### C.1 IC / rankIC / ICIR

- **为什么**：第一道标尺——因子排序准不准（IC）、稳不稳（ICIR）。比净值更基础，先看它。
- **做什么**：
  1. 每个调仓日，取当日因子值和未来收益，只保留可投资的票
  2. `IC[t] = corr(因子, 未来收益)`（Pearson）；`rankIC[t] = spearman`
  3. 得到 IC 时间序列；`IC均值 = mean`、`ICIR = mean/std`
- **产出**：`backtest/<factor>/ic_series.parquet`、汇总 `ic_summary.json`
- **对应程序**：`src/backtest/ic.py` 里的 `compute_ic(factor_panel, fwd_ret, mask)`
- **学习点**：横截面相关（`.corrwith`）；时间序列统计；★区分"每期算再平均" vs "整体一个大相关"（后者是错的）。
- **自检**：符号对不对（振幅/反转负、APM正）；|rankIC| 略大于 |IC|；量级和研报同档。

### C.2 分组与多空对冲

- **为什么**：把因子变成"可交易的组合"，看它实际能赚多少。
- **做什么**：
  1. 每个调仓日，按因子值把可投资票排序、切 5 组（quintile）
  2. 组内**等权**
  3. 每组下一期收益 = 组内股票未来收益的等权平均
  4. 多空对冲：负向因子 = Q1 − Q5；正向因子 = Q5 − Q1（方向跟 IC 符号走）
  5. long-only：多头组 − 全体均值（研报的"多头对冲各组均值"口径）
- **产出**：`backtest/<factor>/group_returns.parquet`（每组每期收益）、`ls_returns.parquet`（多空序列）
- **对应程序**：`src/backtest/portfolio.py` 里的 `quantile_returns()`、`long_short()`
- **学习点**：`pd.qcut` 分组；`groupby` 聚合；★方向参数化（别写死买哪组，跟因子符号走）；等权 vs 加权。
- **自检**：5 组年化收益应大致单调（理想振幅这类好因子）；多空收益为正（方向对了）。

### C.3 净值曲线与绩效指标

- **为什么**：把每期收益串成净值，算出研报表格里的那套指标，才能和研报逐项对齐。
- **做什么**：
  1. 净值 `nav = (1 + 收益序列).cumprod()`
  2. 年化收益、年化波动、**IR = 年化收益/年化波动**、最大回撤、月度胜率、换手率
  3. 扣**双边千三**交易成本（换手 × 费率）
- **产出**：`backtest/<factor>/nav.parquet`、`metrics.json`
- **对应程序**：`src/backtest/metrics.py`（`annual_return / annual_vol / max_drawdown / win_rate / turnover`）
- **学习点**：累计乘积（`cumprod`）；最大回撤算法（`(nav/nav.cummax()−1).min()`）；换手率定义（前后持仓权重变化）；交易成本建模。
- **自检**：净值形状、IR 量级和研报图对得上；扣费后仍为正。

### C.4 中性化（进阶口径）

- **为什么**：研报的月报口径是"行业市值中性"。裸因子可能只是变相的市值/行业暴露；中性化后剩下的才是"纯 alpha"。
- **做什么**：每个调仓日，把因子对 `log流通市值 + 行业哑变量` 做横截面回归，取**残差**作为中性化后的因子，再跑一遍 C.1–C.3。
- **产出**：`factors/<factor>_neutral.parquet` + 对应回测结果
- **对应程序**：`src/backtest/neutralize.py` 里的 `neutralize(factor, mv, industry)`
- **学习点**：横截面回归取残差（`statsmodels.OLS`）；哑变量编码（`pd.get_dummies`）；"中性化 = 剔除已知风格暴露"的思想。
- **自检**：中性化后 IC 通常略降但更稳；和研报的中性化后指标对齐。

---

## 阶段D：合成因子

- **为什么**：单因子各有强弱和失效期，合成能提升稳定性（研报合成 IR 显著高于单因子）。
- **做什么**：
  1. 每个调仓日，四因子各自在**行业内**去极值（winsorize）+ 标准化（z-score）
  2. **方向对齐**：把负向因子乘 −1（或统一成"越大越好"）
  3. **权重**：过去 12 期各因子 ICIR 作为权重，加权求和
  4. 合成因子再跑一遍阶段C
- **产出**：`factors/composite.parquet` + `backtest/composite/*`
- **对应程序**：`src/factors/composite.py`
- **学习点**：去极值方法（MAD/3σ）；分组内标准化（`groupby().transform`）；滚动权重（`rolling` ICIR）；★方向对齐——四因子符号不统一，合成前必须摆正，否则互相抵消。
- **自检**：合成因子 IC/IR 应高于任一单因子；分池（沪深300/中证500/中证1000/国证2000）表现——中小盘更优（对齐研报）。

---

## 阶段E：样本外诊断（OOS）

- **为什么**：研报样本大多止于 2020。用 2020–2026 做真正的样本外检验，才知道因子现在还灵不灵；重点解释 2025.7–2026.6 疲软期。
- **做什么**：
  1. 分样本内（≤2020）/样本外（2020–2026）分别统计绩效，看衰减
  2. 疲软期归因：把合成因子月收益和市值因子月收益对比，算相关性；看是否"大盘风格占优时因子失效"
- **产出**：`backtest/oos_report/*`（分段绩效表、归因图）
- **对应程序**：`notebooks/oos_diagnosis.ipynb`
- **学习点**：样本内外划分；因子拥挤/风格切换归因；相关性分析。
- **自检**：能否用数据支撑"疲软期 vs 市值风格切换"的关联（这正是你简历那句话的底气）。

---

## 阶段F：可视化与网站

> 把回测结果变成一个**每日/定期自动更新**的网站。你简历上写了"制作网站"，这一层就是把它做实。

### F.1 图表数据准备

- **为什么**：网站不该在浏览器里现算回测（慢、重）。应该在后端把要展示的数据**预先算好、存成轻量格式**，前端只负责画。
- **做什么**：把阶段C-E 的结果整理成前端要用的最小数据集：各因子净值序列、IC 序列、月度收益、分池表现、绩效指标汇总。
- **产出**：`website/data/*.json`（每张图一个 json，轻量、前端直接读）
- **对应程序**：`src/website/prepare_data.py`
- **学习点**：**前后端分离**思想——重计算在后端、展示在前端；`DataFrame.to_json`；数据瘦身（只存画图要用的点）。

### F.2 网站生成（静态站）

- **为什么**：因子跟踪网站是"展示型、数据定期更新"，不需要后端服务器。**静态网站**（纯 HTML/JS，读预生成的 json）最简单、免服务器、能白嫖 GitHub Pages。
- **做什么**：
  1. 一个 HTML 模板 + 一个画图库（图表用 ECharts/Chart.js，纯前端）
  2. JS 读 `website/data/*.json`，渲染净值曲线、IC 柱状、分池对比、绩效表
  3. 页面分区：四个单因子 + 合成因子 + 分池 + 最新一期快照
- **产出**：`website/index.html` + `website/assets/*`（可直接本地打开预览）
- **对应程序**：`src/website/build_site.py`（把数据注入模板）或直接手写模板
- **学习点**：静态站 vs 动态站；前端画图库；HTML/CSS/JS 基础；模板渲染（Jinja2）。

### F.3 GitHub 上传与自动部署

- **为什么**：GitHub Pages 能把静态网站免费托管成一个公开 URL；配合定时任务能"每天自动更新"。
- **做什么**：
  1. `git init` → 建仓库 → 把 `website/` 推上去（★注意 `.gitignore` 已排除 token 和 data_raw，别把凭证/大数据推上去）
  2. 仓库设置里开 GitHub Pages，指向 `website/` 或 `docs/`
  3. 自动更新：本地用 launchd（Mac）或 GitHub Actions 定时跑"下载增量→重算→重生成 json→git push"
- **产出**：一个公开 URL（`https://<用户名>.github.io/<仓库>`）
- **对应程序**：`src/website/deploy.sh` + `.github/workflows/update.yml`（如用 Actions）
- **学习点**：git 基础（init/add/commit/push）；★凭证安全（token 绝不进仓库）；GitHub Pages；CI/CD 定时任务（cron/launchd/Actions）；增量更新流水线。
- **自检**：推上去前先 `git status` 确认没带 `.tushare_token` 和 `data_raw/`；本地 `open website/index.html` 预览无误再部署。

---

## 附录：能力学习地图（这次复现你会系统学到什么）

| 环节 | 编程/工具 | 数据分析概念 | 金融概念 |
|---|---|---|---|
| 清洗层 | pandas 宽表/pivot、布尔掩码、时点查询 | 复权、防前视、可投资域 | 幸存者偏差、除权 |
| 因子层 | 滚动窗口、分位分组、NaN控制 | 横截面运算、替代口径验证 | 各因子经济逻辑 |
| 回测层 | groupby、cumprod、OLS残差 | IC/ICIR、分组、回撤、中性化 | 多空对冲、风格中性 |
| 合成层 | rolling、transform、去极值 | 因子合成、权重设计 | 因子拥挤、分散化 |
| 诊断 | 相关分析、分段统计 | 样本内外、归因 | 风格切换、因子失效 |
| 网站 | HTML/JS/ECharts、git、CI | 前后端分离、数据瘦身 | 因子监控 |

---

## 执行顺序建议（务实版）

```
1. 阶段A 全做（清洗层，一次到位）           ← 地基
2. 阶段B.1 理想振幅（第一个因子）
3. 阶段C 全做（用理想振幅把回测框架跑通）    ← 框架立起来
   ★到这里你已经能产出第一张完整绩效表，最有成就感
4. 阶段B.2 理想反转（替代口径 + 验证）
5. 分钟数据方案 → B.3 聪明钱、B.4 APM
6. 阶段D 合成 → 阶段E 诊断
7. 阶段F 网站上线
```

**每一层做完先自检、再进下一层。** 有任何一步"为什么这么做"不清楚，停下来问——这份文档的意义就是让你每一步都能自己做决策，而不是照着跑。
```
